#include <stdio.h>

int main() {
    int n, i, sum = 0;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    for (i = 0; i <= 2 * n; i += 6) {
        sum += i;
    }

    printf("Sum of even numbers divisible by 3 from 0 to %d: %d\n", 2 * n, sum);

    return 0;
}
