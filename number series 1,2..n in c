#include <stdio.h>

int main() 
{
    int n;

    
    printf("Enter a number: ");
    scanf("%d", &n);

   
    printf("Number Series: ");
    for (int i = 1; i <= n; i++) 
	{
        printf("%d", i);

       
        if (i < n) 
		{
            printf(", ");
        }
    }

    return 0;
}
